name: ifitworks_app
description: A new Flutter project for Firebase authentication.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjust if needed for your Flutter version

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2 # For some basic icons (optional)
  firebase_core: ^2.24.2 # Firebase initialization
  firebase_auth: ^4.15.3 # Authentication
  cloud_firestore: ^4.13.6 # Firestore (or use firebase_database for Realtime DB)
  # firebase_database: ^10.3.7 # Realtime Database (if preferred over Firestore)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700